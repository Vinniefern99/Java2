package Module_8;

public class Notepad
{

    /* 
    switch (segment)
    {
        case 'A':
        case 'a':   drawHorizontal(topRow);
                    break;
        case 'B':
        case 'B':   drawVertical(rightCol, topRow, bottomRow);
                    break;
        case 'C':
        case 'c':   drawVertical(rightCol, topRow, bottomRow);
                    break;
        case 'D':
        case 'd':   drawVertical(rightCol, topRow, bottomRow);
                    break;
        case 'E':
        case 'e':   drawVertical(rightCol, topRow, bottomRow);
                    break;
        case 'F':
        case 'f':   drawVertical(rightCol, topRow, bottomRow);
                    break;
        case 'G':
        case 'g':   drawVertical(rightCol, topRow, bottomRow);
                    break;
        default: return false; //if no match. shouldn't ever happen
    }
    
    
    
    /*
        //first, clear the image
        clearImage();
        
        String leftFull = "068AaBbCcEeFf";
        String rightFull = "0134789AaDd";
        String leftTop = "459";
        String rightBottom = "56Bb";
        String rightTop = "2";
        String leftBottom = "2Dd";
        String top = "02356789AaCcEeFf";
        String middle = "2345689AaBbDdEeFf";
        String bottom = "023568BbCcDdEe";

        if (leftFull.indexOf(segment) >= 0)
            drawVertical(leftCol, topRow, bottomRow);

        if (rightFull.indexOf(segment) >= 0)
            drawVertical(rightCol, topRow, bottomRow);

        if (leftTop.indexOf(segment) >= 0)
            drawVertical(leftCol, topRow, midRow);

        if (rightBottom.indexOf(segment) >= 0)
            drawVertical(rightCol, midRow, bottomRow);

        if (rightTop.indexOf(segment) >= 0)
            drawVertical(rightCol, topRow, midRow);

        if (leftBottom.indexOf(segment) >= 0)
            drawVertical(leftCol, midRow, bottomRow);

        if (top.indexOf(segment) >= 0)
            drawHorizontal(topRow);

        if (middle.indexOf(segment) >= 0)
            drawHorizontal(midRow);

        if (bottom.indexOf(segment) >= 0)
            drawHorizontal(bottomRow);

             */
    
    
    
}
